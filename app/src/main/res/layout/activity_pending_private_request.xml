<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/pendingPrivateGrpRequests"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tag_layout_bg_color"
    android:orientation="vertical" >

    <include
        android:id="@+id/headerLayout"
        layout="@layout/header" />

    <com.TagFu.pulltorefresh.PullToRefreshListView
        android:id="@+id/pvtpendingsList"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:cacheColorHint="@android:color/white"
        android:divider="@color/twitter_bg_color"
        android:dividerHeight="@dimen/lineheight" />

</LinearLayout>